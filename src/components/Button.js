import React, { Component } from 'react';

class Button extends Component {
  render() {
    return <input className="btn btn-primary btn-xl" type="button" value="ADD" />;
  }
}

export default Button;
